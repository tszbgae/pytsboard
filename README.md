Install Instructions:

1. (Recommended) create a new virtual environment (e.g. python -m venv pytsdashvenv) and enter virtual environment. 

2.  pip install build

3.  python -m build

4. pip install .

5.  at command line, type run-dashboard